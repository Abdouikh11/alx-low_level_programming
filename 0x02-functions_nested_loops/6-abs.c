#include "main.h"

/**
 * _abs - Absolute number
 * Return: Returns the number to it's abosulte
 * @n: The number that we are gonna absolute
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
