#include "main.h"

/**
 * main - Entry point
 * @argc: number of arguments
 * @argv: the array of strings
 * Return: returns 0
 *
 */

int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
